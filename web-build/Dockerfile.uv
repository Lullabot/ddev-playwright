#ddev-generated
# Install uv Python tool runner for running Python-based tools like
# mysql-to-sqlite3 without polluting the system Python environment.
RUN curl -LsSf https://astral.sh/uv/0.11.0/install.sh | UV_INSTALL_DIR=/usr/local/bin sh
