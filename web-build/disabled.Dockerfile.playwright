#ddev-generated
# Debian images by default disable apt caching, so turn it on until we finish
# the build.
RUN mv /etc/apt/apt.conf.d/docker-clean /etc/apt/docker-clean-disabled

# LAYER ORDERING IS LOAD-BEARING — read before rearranging.
#
# The Playwright browser install at the bottom of this file uses
# --mount=type=bind on the copied test directory. BuildKit folds the contents
# of a bind mount into that layer's cache key, so *every* instruction after it
# is invalidated whenever any file under PLAYWRIGHT_TEST_DIR changes — and the
# pre-start hook in config.playwright.yml re-copies that directory on every
# `ddev start`. Editing a single spec file therefore used to reinstall icewm,
# kasmvnc, go-task and uv on every restart.
#
# So: anything NOT derived from the user's Playwright directory goes ABOVE the
# browser install, and the browser install stays last. The same rule is why
# Dockerfile.10-go-task and Dockerfile.20-astral-uv carry numeric prefixes —
# ddev concatenates web-build/Dockerfile.* in alphabetical order, and digits
# sort ahead of letters, so those land before this file.

# libnss3-tools lets the web-entrypoint.d mkcert-nssdb.sh script populate the
# user's NSS database with DDEV's mkcert root CA — required for Chromium-based
# browsers to trust *.ddev.site HTTPS certificates without `ignoreHTTPSErrors`.
#
# This is deliberately a separate RUN from the window manager below: it is
# needed whether or not the VNC/desktop stack is installed, so it must not be
# swept away with it.
#
# Each apt RUN runs its own `apt-get update`. The cache mount on /var/lib/apt
# shadows any package lists baked into an earlier layer, and layers cache
# independently, so a RUN cannot assume a previous one populated the lists.
RUN --mount=type=cache,target=/var/cache/apt,sharing=locked \
  --mount=type=cache,target=/var/lib/apt,sharing=locked \
  sudo apt-get update \
  && sudo apt-get install -y libnss3-tools

# ---------------------------------------------------------------------------
# VNC / desktop stack. Only used for headed local debugging.
# ---------------------------------------------------------------------------

# Install a window manager for headed test runs.
RUN --mount=type=cache,target=/var/cache/apt,sharing=locked \
  --mount=type=cache,target=/var/lib/apt,sharing=locked \
  sudo apt-get update \
  && sudo apt-get install -y icewm \
    xauth

# Install kasmvnc for remote access.
COPY install-kasmvnc.sh /usr/local/bin
RUN chmod +x /usr/local/bin/install-kasmvnc.sh

RUN --mount=type=cache,target=/var/cache/apt,sharing=locked \
  --mount=type=cache,target=/var/lib/apt,sharing=locked \
  install-kasmvnc.sh

# We can't add the user to the ssl-cert group. Rather, we can add them, but it
# won't actually apply to ddev ssh / docker exec.
# Even though we disable requiring SSL because we route through the ddev
# router, kasmvnc still requires reading the TLS cert.
RUN sudo chgrp -R $gid /etc/ssl/private

RUN sudo -u $username mkdir /home/$username/.vnc
COPY kasmvnc.yaml xstartup /home/$username/.vnc
RUN chown $username:$username /home/$username/.vnc/*
RUN sudo -u $username touch /home/$username/.vnc/.de-was-selected

RUN sudo -u $username /bin/bash -c 'echo -e "secret\nsecret\n" | kasmvncpasswd -wo -u $USER'

# ---------------------------------------------------------------------------
# End of VNC / desktop stack.
# ---------------------------------------------------------------------------

# Install Playwright browsers and system dependencies.
#
# THIS MUST BE THE LAST BUILD STEP IN THIS FILE — see the layer ordering note
# at the top.
#
# Manual changes to Dockerfile.playwright will be reverted every time ddev
# start is run. In general, edit this file instead.
#
# We want to install whatever browsers are used by the currently locked version
# of playwright in PLAYWRIGHT_TEST_DIR. What this does is:
#
# 1. Creates a copy of the playwright directory in the image, at the *same*
#    path PLAYWRIGHT_TEST_DIR resolves to at runtime (/var/www/html/__PLAYWRIGHT_TEST_DIR__
#    below — substituted with the real value on the host before the build
#    ever sees this file, since a plain Dockerfile has no way to read a host
#    env var; see the pre-start hook in config.playwright.yml that does the
#    substitution). The path has to match exactly, because Playwright keeps a
#    hidden .links/<hash> file keyed on the install path to know what browsers
#    are installed by what apps — install at the wrong path here and
#    Playwright won't recognize the real runtime path as already having
#    browsers, and will re-download them there even though they're already in
#    the image. As the Docker context is the web-build directory, we have a
#    pre-start hook in config.playwright.yml that stages the current
#    dependency manifests there. It stages only the manifests -- specs,
#    fixtures, and snapshots are not needed to install browsers, and this tree
#    is bind-mounted, so anything staged here lands in the cache key for this
#    layer and every layer after it.
# 2. $username doesn't work in --mount... lines as it's a shell variable set by
#    ddev. Instead, we mount the .cache cache into /ms-playwright-cache, and
#    symlink it in. We merge any existing ~/.cache content into the mount first
#    (cp -an preserves existing cache entries) and then replace ~/.cache with a
#    symlink. The merge step makes this order-independent with other Dockerfile
#    instructions that may populate ~/.cache before this RUN executes.
# 3. PLAYWRIGHT_SKIP_BROWSER_GC=1 prevents `playwright install` from removing
#    browsers installed by a different playwright-core version (e.g.
#    @playwright/cli from ddev-playwright-cli). Without this, playwright's
#    built-in GC deletes any browsers not referenced by the current package.
#    See https://github.com/microsoft/playwright/blob/7f55e117ce4d/packages/playwright-core/src/server/registry/index.ts#L1080-L1081
# 4. Then we remove the symlink, and copy the cache directory into ~/.cache so
#    it is persisted in the image.
#
# See https://github.com/ddev/ddev/issues/4727.
RUN --mount=type=cache,target=/var/cache/apt,sharing=locked \
  --mount=type=cache,target=/var/lib/apt,sharing=locked \
  --mount=type=cache,target=/ms-playwright-cache,sharing=locked \
  --mount=type=bind,source=./playwright,target=/playwright \
  sudo -u $username mkdir -p /var/www/html/__PLAYWRIGHT_TEST_DIR__ \
  && cp -a /playwright/. /var/www/html/__PLAYWRIGHT_TEST_DIR__ \
  && chown -R $username:$username /var/www/html/__PLAYWRIGHT_TEST_DIR__ \
  && chown -R $username:$username /ms-playwright-cache \
  && cp -an /home/$username/.cache/* /ms-playwright-cache/ 2>/dev/null; \
  rm -rf /home/$username/.cache \
  && sudo -u $username ln -s /ms-playwright-cache /home/$username/.cache \
  && cd /var/www/html/__PLAYWRIGHT_TEST_DIR__ \
  && if [ -f yarn.lock ]; then \
       export JS_PKG_MANAGER=yarn; \
       corepack enable; \
       sudo -u $username $JS_PKG_MANAGER; \
     else \
       export JS_PKG_MANAGER=npx; \
       sudo -u $username npm install; \
     fi \
  && sudo -u $username $JS_PKG_MANAGER playwright install-deps \
  && sudo -u $username PLAYWRIGHT_SKIP_BROWSER_GC=1 $JS_PKG_MANAGER playwright install \
  && rm -rf /var/www/html/__PLAYWRIGHT_TEST_DIR__ \
  && rm /home/$username/.cache \
  && sudo -u $username mkdir -p /home/$username/.cache \
  && sudo -u $username cp -a /ms-playwright-cache/* /home/$username/.cache/

# We're done with apt so disable caching again for the final image.
RUN mv /etc/apt/docker-clean-disabled /etc/apt/apt.conf.d/docker-clean
